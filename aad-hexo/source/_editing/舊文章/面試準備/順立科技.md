```
const { getA , setA} = initScore(0);
const { getB , setB} = initScore(100);

求  setA(0)  ;  // 0
    setB()
    setA(3) 
    get(3)        // 3
    getB(100)  ;  // 100


    function init(i) {
        function act1 (x) {
            return x = i
        }

        function act2 () {

        }
        return [act1 , act2];
    }
```


求input btn  type 12345 to ***** 轉換


求 
```
var arr = [100, 200, 400];

for(var i=0; i < 3; i++) {
    setTimeout(function () {
        console.log(arr[i]);
    }, arr[i])
}

輸出的是 
```